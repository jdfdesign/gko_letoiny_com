<div class="container">
  <div class="row">
    <article class="header">
      <%= render_slideshow_for(parent) %>
      <%= render_page_title(parent.title) if resource.show_title_in_page %>
      <%= render_copy_text(parent.body) %>
    </article>
  </div>
  <%= render collection %>
</div>